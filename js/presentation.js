(function(){const root=document.documentElement;const slides=[...document.querySelectorAll('.slide')];if(!slides.length)return;let i=Math.max(0,slides.findIndex(s=>s.classList.contains('active')));if(i<0)i=0;let revealIndex=0;const idEl=document.querySelector('[data-role="slide-id"]');function current(){return slides[i]}function update(){slides.forEach((s,n)=>s.classList.toggle('active',n===i));revealIndex=0;current().querySelectorAll('.reveal').forEach(x=>x.classList.remove('shown'));if(idEl)idEl.textContent=current().dataset.slideId||'';history.replaceState(null,'','#'+(current().dataset.slideId||''))}function next(){let r=[...current().querySelectorAll('.reveal')];if(revealIndex<r.length){r[revealIndex++].classList.add('shown');return}if(i<slides.length-1){i++;update()}}function prev(){if(i>0){i--;update()}}function gotoMap(){let idx=slides.findIndex(s=>s.dataset.kind==='map');if(idx>=0){i=idx;update()}}document.querySelectorAll('[data-action="next"]').forEach(b=>b.addEventListener('click',next));document.querySelectorAll('[data-action="prev"]').forEach(b=>b.addEventListener('click',prev));document.querySelectorAll('[data-action="map"]').forEach(b=>b.addEventListener('click',gotoMap));document.querySelectorAll('[data-action="home"]').forEach(b=>b.addEventListener('click',()=>location.href=(b.dataset.href||'../../hub.html')));document.querySelectorAll('[data-action="full"]').forEach(b=>b.addEventListener('click',()=>{if(!document.fullscreenElement){document.documentElement.requestFullscreen?.()}else{document.exitFullscreen?.()}}));addEventListener('keydown',e=>{if(['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName))return;if(e.key==='ArrowRight'||e.key===' '){e.preventDefault();next()}else if(e.key==='ArrowLeft'){prev()}else if(e.key==='Home'){e.preventDefault();location.href=document.body.dataset.home||'../../hub.html'}else if(e.key.toLowerCase()==='m'){gotoMap()}else if(e.key.toLowerCase()==='f'){if(!document.fullscreenElement)document.documentElement.requestFullscreen?.();else document.exitFullscreen?.()}});let hash=location.hash.replace('#','');if(hash){let idx=slides.findIndex(s=>s.dataset.slideId===hash);if(idx>=0)i=idx}update();if(!matchMedia('(prefers-reduced-motion: reduce)').matches){setInterval(()=>document.querySelectorAll('.term-swap').forEach(t=>t.classList.toggle('swap')),3200)}})();